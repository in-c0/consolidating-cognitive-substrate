# Reconciliation — 2026-09-02

First reconciliation. Establishes the baseline programme state.

## Method

`gh api` GET requests against each declared sibling slug, plus a read of
`in-c0/state-promotion` at `3495d49`. No sibling repository was modified.

## Repository existence — correction to the programme brief

The 2026-09-02 programme brief stated that `in-c0/state-promotion`,
`in-c0/modular-consolidation` and `in-c0/lifetime-integrity` exist, and that
`adaptive-commitment` and `plasticity-routing` do not.

**Observed:**

| Repository | Brief said | Observed 2026-09-02 |
|---|---|---|
| `in-c0/state-promotion` | exists | ✅ exists, public, head `3495d49` |
| `in-c0/modular-consolidation` | exists | ❌ **not found** |
| `in-c0/lifetime-integrity` | exists | ❌ **not found** |
| `in-c0/adaptive-commitment` | not yet created | ❌ not found (as expected) |
| `in-c0/plasticity-routing` | not yet created | ❌ not found (as expected) |

`modular-consolidation` and `lifetime-integrity` returned
`Could not resolve to a Repository` and did not appear in a full listing of the
owner's 100+ repositories. The token carries `repo` scope and lists private
repositories (several appear in the listing), so this is not a visibility artefact.

**Decision:** both are recorded as `planned` nodes, consistent with the brief's
instruction to record non-existent repositories as planned rather than assume
their contents. Four of five siblings are therefore planned; one exists.

If either repository exists under a different name or owner, the next
reconciliation should correct the registry. `tools/reconcile.py` raises DRIFT if a
repository marked `planned` is later found to exist.

## `state-promotion` — observed state

- Head `3495d49`, public, default branch `main`, pushed 2026-09-02T00:39:14Z
- Self-declared status: *"pre-results / experiment scaffold. No empirical claim is made yet."*
- `results/` contains only `.gitkeep` — **no results exist**

| Experiment | Stage |
|---|---|
| EXP-000 | pilot complete; explicitly not paper evidence |
| EXP-001 | pre-result scaffold; prereg is a DRAFT LOCK candidate; Amendments A–E locked pre-result |
| EXP-002 | planned (CITB/TRACE) |
| EXP-003 | planned (~7B replication) |

### Assessment of methodological discipline

The repository's pre-result audit trail is unusually strong, and the umbrella
adopts its rules programme-wide (see
[EVIDENCE-MAP.md](../EVIDENCE-MAP.md#inherited-admissibility-rules)). Notably, it
self-identified and corrected before any LM score was inspected:

- held-out evaluation labels reaching the promotion gate (Amendment B)
- optimizer state discarded at segment boundaries, weakening its own baselines (Amendment C)
- latent state enabled for B5 but not for B3/B4, confounding the routing comparison (Amendment D)
- decision-time routing compute being mistakable for matched compute (Amendment E)

Amendments C and D are corrections that *strengthened the baselines against the
repository's own hypothesis*. That is the behaviour the programme should preserve.

### Disagreement with the ledger

None. The repository claims nothing, and the ledger records nothing as supported.

## EXP-000 classification

EXP-000 is recorded as **inadmissible**, matching its own declaration. Two reasons,
both from its result document:

1. The promotion arm tied with replay within pilot variance (0.457 ± 0.057 vs
   0.459 ± 0.058 final average) — a null result on the central mechanism.
2. The fixed-consolidation arm was algebraically identical to replay by
   construction, so the pilot could not have adjudicated the claim in either
   direction.

The ledger therefore records CCS-C2 as `unresolved`, not `pilot-supported`.

## Terminology

No conflicts. `state-promotion` predates the ACCUMULATE/ALLOCATE/COMMIT
vocabulary and uses "promotion" for the COMMIT operator; the mapping is recorded
in [TERMINOLOGY.md](../TERMINOLOGY.md#cross-repository-mapping). No change is
requested of that repository.

## Scope

`in-c0/reference-worlds` was reviewed and is **not** a CCS node: it concerns
reference-anchored visual world synthesis. It shares the word "persistence" but
not the substrate, claims, or evaluation protocol. Recorded in
`ledger/repos.json` under `not_in_programme`.

## Programme state

- 10 claims defined; **0 with empirical support**; 0 confirmatory
- 6 `theoretical-conjecture`, 4 `unresolved`
- 5 of 10 claims have **no runnable test anywhere** (their repositories do not exist)
- 9 DAG nodes; 1 capable of reading out (`state-promotion/EXP-001`)
- Synthesis readiness: **0 of 4 tiers**, 0 checkboxes cleared
- Integration experiment `ccs/EXP-I1`: no repository, no design

## Open questions for the next reconciliation

1. Do `modular-consolidation` and `lifetime-integrity` exist elsewhere, or was the
   brief mistaken? The registry currently assumes the latter.
2. Has `state-promotion/EXP-001` moved from DRAFT LOCK candidate to a frozen
   protocol with a recorded commit SHA and disjoint confirmatory seed list?
3. Has the development-only calibration phase quantified B5 decision-time routing
   overhead? Amendment E requires this before protocol v1.0 is frozen.
4. Does `results/` remain empty?

## Actions taken

None against sibling repositories, per [RECONCILIATION-POLICY.md](../RECONCILIATION-POLICY.md).
Umbrella registry initialised to observed state.
